/**
 * The final reveal at the very end of the site.
 * Change `name` here when Baby's name is chosen — nothing else needs editing.
 */
export const babyName = {
  label: "Baby's Name",
  /** e.g. "Baby Aarav" — leave as the placeholder until it's decided. */
  name: "Baby [NAME]",
  closingEmoji: "🤍",
};
